- @recipe.each_slice(3) do |r|
  .row
    - r.each do |rec|
      .col-md-4
        .recipe
          .image_wrapper
            = link_to rec do
              = image_tag rec.image.url(:medium)
          %h2= link_to rec.title, rec